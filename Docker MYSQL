sudo docker pull mysql:latest
sudo docker run -d --name mysql-server -p 3306:3306 -e "MYSQL_ROOT_PASSWORD=password" mysql
sudo docker exec -it sql-server bash

mysql -uroot -p

---------------------------------------------------------------
docker pull mysql
docker run --name mysql -e MYSQL_ROOT_PASSWORD=password -d mysql -p 3306:3306
